export const createMainWindow = () => {
  // ToDo: Create a BrowserWindow
  // ToDo: Open UI using window.loadFile on index.html, show window when dom-ready
  // ToDo: Disable nodeIntegration and enable contextIsolation for security reasons
  // ToDo: Open devTools for debugging renderer
  // ToDo: Add preload script for IPC communication
}
