const { ipcRenderer, contextBridge } = require('electron/renderer');

// ToDo: Implement contextBridge and expose a method to get the app version from the main thread

console.log('Preload script executed!');
